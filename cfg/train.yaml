# hydra.job.chdir: false

headless: true

sim: ${task.sim}
env: ${task.env}

model_dir: null
load_history_dir: null
# model_dir: /home/chenjy/Multi-UAV-pursuit-evasion/scripts/outputs/train_0to5_envgen/02-06_16-19/wandb/run-20250206_161955-la6l2s9h/files/checkpoint_1232207872.pt
# load_history_dir: /home/chenjy/Multi-UAV-pursuit-evasion/scripts/outputs/train_0to5_envgen/02-06_16-19/wandb/run-20250206_161955-la6l2s9h/files/history_9400.npy


total_frames: 10000_000_000 #1000_000_000
max_iters: -1
eval_interval: -1
save_interval: 100
seed: 0

viewer:
  resolution: [960, 720]
  # up
  eye: [-0.005, 0.0, 5.]
  lookat: [0., 0., 1.]
  # eye: [-2.5, 0.0, 2.]
  # lookat: [0., 0.0, 0.1]

wandb:
  group: ${oc.select:..task.name}
  run_name: train_0to5_envgen_load
  # run_name: eval
  entity: chenjy
  project: RAL
  # mode: disabled
  mode: online # set to 'disabled' when debugging locally, online
  run_id: 
  monitor_gym: True
  tags:

defaults:
  # - task: HideAndSeek
  - task: HideAndSeek_envgen
  - algo: mappo
  - _self_