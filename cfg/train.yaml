# cl
prob_curriculum: 0.9
prob_decay: 0.5

# model_dir: /home/jiayu/OmniDrones/scripts/outputs/size0_7_vel1_3_detect0_8_static/02-10_10-12/wandb/run-20240210_101225-hpf71avr/files/checkpoint_1546780672.pt
model_dir: null

# hydra.job.chdir: false

headless: true

sim: ${task.sim}
env: ${task.env}

total_frames: 100_000_000_000
max_iters: -1
eval_interval: 200
save_interval: 200
seed: 0

viewer:
  resolution: [960, 720]
  eye: [4., 0., 4.]
  lookat: [0., 0., 1.]

wandb:
  group: ${oc.select:..task.name}
  run_name: size0_7_vel1_3_detect0_8_oldreward_ratecontroller_circle
  # run_name: debug
  job_type: train
  entity: chenjy
  project: PredatorPrey
  mode: online
  # mode: disabled # set to 'disabled' when debugging locally
  run_id: 
  monitor_gym: True
  tags: 

defaults:
  # - task: HideAndSeek
  # - task: HideAndSeek_static
  # - task: HideAndSeek_sparse
  - task: HideAndSeek_circle
  - algo: mappo
  - _self_
