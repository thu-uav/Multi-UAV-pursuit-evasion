model_dir: /home/chenjy/OmniDrones/scripts/outputs/inner_cylinder0to5_validation.pt
# model_dir: /home/chenjy/OmniDrones/scripts/outputs/warmup_cylinder0.pt
# model_dir: /home/chenjy/OmniDrones/scripts/outputs/warmup_cylinder1to3.pt
# model_dir: null

# hydra.job.chdir: false

headless: true

sim: ${task.sim}
env: ${task.env}

total_frames: 100_000_000_000
max_iters: -1
eval_interval: -1
save_interval: 200
seed: 0

viewer:
  resolution: [960, 720]
  # # up
  # eye: [0.01, 0.01, 6.]
  # lookat: [0., 0., 2.]
  # # debug
  # eye: [0., -4., 4.]
  # lookat: [0., 0., 1.]
  # front
  eye: [20., 0., 20.]
  lookat: [0., 0., 1.]

wandb:
  group: ${oc.select:..task.name}
  run_name: cylinder5_validation_check_catch015
  # run_name: debug
  # run_name: cylinder0to5_fromscratch_catch0_5to0_15_wo_validation
  job_type: train
  entity: chenjy
  project: PredatorPrey
  mode: online
  # mode: disabled # set to 'disabled' when debugging locally
  run_id: 
  monitor_gym: True
  tags: 

defaults:
  # - task: HideAndSeek
  # - task: HideAndSeek_circle_static
  - task: HideAndSeek_circle_static_UED
  # - task: HideAndSeek_circle_eval
  - algo: mappo
  - _self_
