# hydra.job.chdir: false

headless: true

sim: ${task.sim}
env: ${task.env}

# model_dir: /home/jiayu/OmniDrones/scripts/outputs/catch0_5_TP_window1/07-19_12-00/wandb/run-20240719_120034-qehz35x7/files/checkpoint_629407744.pt
# model_dir: /home/jiayu/OmniDrones/scripts/outputs/empty_catch0_8_v1_0_TP_add_dist_mask/08-04_01-13/wandb/run-20240804_011313-iglyim8s/files/checkpoint_327811072.pt
model_dir: null

total_frames: 3000_000_000 #1000_000_000
max_iters: -1
eval_interval: -1
save_interval: 100
seed: 0

viewer:
  resolution: [960, 720]
  # up
  # eye: [-0.005, 0.0, 5.]
  # lookat: [0., 0., 1.]
  eye: [-2.5, 0.0, 2.]
  lookat: [0., 0.0, 0.1]

wandb:
  group: ${oc.select:..task.name}
  run_name: GAN0to5_catch0_3_v1_3_TP
  # run_name: debug
  job_type: train
  entity: chenjy
  project: RAL
  # mode: disabled
  mode: online # set to 'disabled' when debugging locally, online
  run_id: 
  monitor_gym: True
  tags:

defaults:
  # - task: HideAndSeek_square_partial
  # - task: HideAndSeek_circle_partial_woTP
  # - task: HideAndSeek_circle_partial_TP
  - task: HideAndSeek_circle_partial_TP_GAN
  - algo: mappo
  - _self_
