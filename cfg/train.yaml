# hydra.job.chdir: false

headless: true

sim: ${task.sim}
env: ${task.env}

# model_dir: /home/jiayu/OmniDrones/scripts/outputs/goto_static_wosmooth/06-23_16-27/wandb/run-20240623_162749-lykl5nry/files/checkpoint_final.pt
model_dir: null

total_frames: 3000_000_000 #1000_000_000
max_iters: -1
eval_interval: -1
save_interval: 100
seed: 0

viewer:
  resolution: [960, 720]
  # front
  eye: [0.005, 0., 4.]
  lookat: [0., 0., 1.]
  # # follow drone
  # eye: [0., 2.0, 2.5]
  # lookat: [0., 0., 1.0]

wandb:
  group: ${oc.select:..task.name}
  # run_name: debug
  run_name: goto_static_smooth1
  job_type: train
  entity: chenjy
  project: sim2real
  # mode: disabled
  mode: online # set to 'disabled' when debugging locally, online
  run_id: 
  monitor_gym: True
  tags: 

defaults:
  - task: Goto_static
  # - task: Track
  # - task: Exchange
  # - task: MultiGoto
  - algo: mappo
  - _self_
