# cl
prob_curriculum: 0.9
prob_decay: 0.5

# model_dir: /home/tangwenhao/workspace/tangwh/OmniDrones/scripts/wandb/run-20231231_004928-1tkpplyy/files/checkpoint_2500.pt
model_dir: null

# hydra.job.chdir: false

headless: true

sim: ${task.sim}
env: ${task.env}

total_frames: 100_000_000_000
max_iters: -1
eval_interval: 10
save_interval: 10
seed: 0

viewer:
  resolution: [960, 720]
  eye: [4., 0., 4.]
  eye: [4., 0., 1.]

wandb:
  group: ${oc.select:..task.name}
  run_name: debug
  job_type: train
  entity: chenjy
  project: PredatorPrey
  mode: online
  # mode: disabled # set to 'disabled' when debugging locally
  run_id: 
  monitor_gym: True
  tags: 

defaults:
  # - task: HideAndSeek
  - task: HideAndSeek_static
  - algo: mappo
  - _self_
