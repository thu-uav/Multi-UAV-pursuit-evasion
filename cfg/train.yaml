# cl
prob_curriculum: 0.9
prob_decay: 0.5

# model_dir: /home/chenjy/OmniDrones/scripts/outputs/circle_vel1_3.pt
model_dir: null

# hydra.job.chdir: false

headless: true

sim: ${task.sim}
env: ${task.env}

total_frames: 100_000_000_000
max_iters: -1
eval_interval: 200
save_interval: 200
seed: 0

viewer:
  resolution: [960, 720]
  eye: [4., 0., 4.]
  lookat: [0., 0., 1.]

wandb:
  group: ${oc.select:..task.name}
  # run_name: size1_0_vel1_3_catch0_5_circle_static
  run_name: 003_debug
  job_type: train
  entity: chenjy
  project: PredatorPrey
  # mode: online
  mode: disabled # set to 'disabled' when debugging locally
  run_id: 
  monitor_gym: True
  tags: 

defaults:
  # - task: HideAndSeek
  - task: HideAndSeek_circle_static
  # - task: HideAndSeek_circle
  # - task: HideAndSeek_multiprey
  - algo: mappo
  - _self_
