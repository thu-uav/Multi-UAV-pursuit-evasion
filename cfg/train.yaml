# hydra.job.chdir: false

headless: true

sim: ${task.sim}
env: ${task.env}

# model_dir: /home/jiayu/OmniDrones/scripts/outputs/goto_static_smooth10_woorder2/06-24_13-48/wandb/run-20240624_134840-mtktp6kf/files/checkpoint_2490630144.pt
# model_dir: /home/jiayu/OmniDrones/scripts/outputs/Track_smooth05_simopt_2stage_loadsmooth0/06-27_19-35/wandb/run-20240627_193552-bdew7qls/files/checkpoint_1179910144.pt
model_dir: null

total_frames: 3000_000_000 #1000_000_000
max_iters: -1
eval_interval: -1
save_interval: 100
seed: 0

viewer:
  resolution: [960, 720]
  # front
  eye: [0., -0.005, 6.]
  lookat: [0., 0., 1.]
  # # follow drone
  # eye: [0., 2.0, 2.5]
  # lookat: [0., 0., 1.0]

wandb:
  group: ${oc.select:..task.name}
  run_name: Track_smooth05_Tm001
  # run_name: debug
  job_type: train
  entity: chenjy
  project: sim2real
  # mode: disabled
  mode: online # set to 'disabled' when debugging locally, online
  run_id: 
  monitor_gym: True
  tags: 

defaults:
  - task: Track
  # - task: MultiGoto
  # - task: Goto_static
  # - task: Exchange
  - algo: mappo
  - _self_
