# hydra.job.chdir: false

headless: true

sim: ${task.sim}
env: ${task.env}

model_dir: /home/jiayu/OmniDrones/scripts/outputs/takeoff_bodyrate1_0_thrust0_9/05-09_21-42/wandb/run-20240509_214214-ztb340s6/files/checkpoint_final.pt
# model_dir: null

total_frames: 3000_000_000 #1000_000_000
max_iters: -1
eval_interval: 100
save_interval: 100
seed: 0

viewer:
  resolution: [960, 720]
  eye: [-3., 0., 3.]
  lookat: [0., 0., 1.]

wandb:
  group: ${oc.select:..task.name}
  # run_name: eval_ours
  run_name: Hover_yaw0_bodyrate1_thrust09
  job_type: train
  entity: chenjy
  project: Track
  # mode: disabled
  mode: online # set to 'disabled' when debugging locally, online
  run_id: 
  monitor_gym: True
  tags: 

defaults:
  - task: Hover
  # - task: Turn
  - algo: mappo
  - _self_
