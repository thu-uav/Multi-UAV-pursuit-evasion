# hydra.job.chdir: false

headless: true

sim: ${task.sim}
env: ${task.env}

# model_dir: /home/jiayu/OmniDrones/scripts/outputs/random0to5_catch0_3_v1_3_arena0_9.pt
# model_dir: /home/jiayu/OmniDrones/scripts/outputs/GAN0to5_TP/08-11_20-07/wandb/run-20240811_200729-2h1rlusg/files/checkpoint_1953103872.pt
model_dir: null

total_frames: 3000_000_000 #1000_000_000
max_iters: -1
eval_interval: -1
save_interval: 100
seed: 0

viewer:
  resolution: [960, 720]
  # up
  eye: [-0.005, 0.0, 20.]
  lookat: [0., 0., 1.]
  # eye: [-2.5, 0.0, 2.]
  # lookat: [0., 0.0, 0.1]

wandb:
  group: ${oc.select:..task.name}
  # run_name: debug
  run_name: GAN0to5_woTP
  # run_name: random0to5
  job_type: train
  entity: chenjy
  project: RAL
  # mode: disabled
  mode: online # set to 'disabled' when debugging locally, online
  run_id: 
  monitor_gym: True
  tags:

defaults:
  # - task: HideAndSeek_square_partial
  # - task: HideAndSeek_circle_partial_woTP
  # - task: HideAndSeek_circle_partial_TP
  - task: HideAndSeek_circle_partial_TP_GAN
  - algo: mappo
  - _self_
