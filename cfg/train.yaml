# model_dir: /home/chenjy/OmniDrones/scripts/outputs/DR_catch0_15_collision001_validation.pt
model_dir: /home/chenjy/OmniDrones/scripts/outputs/cl_catch0_15_collision001_validation.pt
# model_dir: /home/chenjy/OmniDrones/scripts/outputs/CL_v2_omega0_5_validation_cylinder0to5_catch0_15_v1_4_load_v1_3/03-14_21-31/wandb/run-20240314_213120-wr49lk1f/files/checkpoint_1179779072.pt
# model_dir: null

# hydra.job.chdir: false

headless: true

sim: ${task.sim}
env: ${task.env}

total_frames: 100_000_000_000
max_iters: -1
eval_interval: -1
save_interval: 20
seed: 0

viewer:
  resolution: [960, 720]
  # # up
  # eye: [0.001, 0.0, 6.]
  # lookat: [0., 0., 2.]
  # # debug
  # eye: [0., -4., 4.]
  # lookat: [0., 0., 1.]
  # front
  eye: [4., 0., 4.]
  lookat: [0., 0., 1.]

wandb:
  group: ${oc.select:..task.name}
  run_name: omega0_0_load_checktasks_v_1_5_5000
  # run_name: debug
  job_type: train
  entity: chenjy
  project: PredatorPrey
  mode: online
  # mode: disabled # set to 'disabled' when debugging locally
  run_id: 
  monitor_gym: True
  tags: 

defaults:
  # - task: HideAndSeek
  # - task: HideAndSeek_circle_static_UED_cl
  - task: HideAndSeek_circle_static_UED_cl_v2
  # - task: HideAndSeek_circle_static_UED
  # - task: HideAndSeek_circle_eval
  - algo: mappo
  - _self_
